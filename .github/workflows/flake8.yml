name: Flake8

on: [push, pull_request]

jobs:
  flake8:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Install flake8
        run: python3 -m pip install flake8
      - name: Run flake8
        run: flake8 .
